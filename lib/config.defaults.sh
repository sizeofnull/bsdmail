selfsigned="${selfsigned:-no}"
allow_suboptimal_ciphers="${allow_suboptimal_ciphers:-yes}"
mailbox_format="${mailbox_format:-maildir}"
allowed_protocols="${allowed_protocols:- imap pop3 }"

use_cert_config="${use_cert_config:-no}"
country_name="${country_name:-}"
state_or_province_name="${state_or_province_name:-}"
organization_name="${organization_name:-}"
